SwishResults->getRemovedStopwords -- Get an array of stopwords removed from
the query

  array SwishResults->getRemovedStopwords(string index_name)~

This function is EXPERIMENTAL. The behaviour of this function, its name, and
surrounding documentation may change without notice in a future release of
PHP. This function should be used at your own risk.

{index_name} The name of the index used to initialize Swish object.

Returns array of stopwords removed from the query.

vim:ft=help:

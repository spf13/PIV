descriptor->free -- Frees resources associated with descriptor

  bool descriptor->free()~

|descriptor->free| frees resources associated with descriptor, previously
allocated with |oci_new_descriptor|.

Returns TRUE on success or FALSE on failure.

vim:ft=help:

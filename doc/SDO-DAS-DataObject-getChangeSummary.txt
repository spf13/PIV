SDO_DAS_DataObject::getChangeSummary -- Get a data object's change summary

  SDO_DAS_ChangeSummary SDO_DAS_DataObject::getChangeSummary()~

This function is EXPERIMENTAL. The behaviour of this function, its name, and
surrounding documentation may change without notice in a future release of
PHP. This function should be used at your own risk.

Get the SDO_DAS_ChangeSummary for an SDO_DAS_DataObject, or NULL if it does
not have one.

None.

Returns the SDO_DAS_ChangeSummary for an SDO_DAS_DataObject, or NULL if it
does not have one.

vim:ft=help:
